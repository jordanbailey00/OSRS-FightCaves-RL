# `fight-caves-demo-lite` archival note

`fight-caves-demo-lite` is frozen as fallback/reference only.

## Current role

- preserve PR 7.1 headed validation evidence
- preserve the lite-demo starter-state and logging artifacts as historical reference
- remain available as a fallback module while the RSPS-backed headed demo path is brought up

## Not the active path

- This module is not the active primary Phase 7 headed-demo path.
- The active Phase 7 path is the RSPS-backed Fight Caves demo built from `RSPS` plus stock `void-client`.
- The old lite-demo PR 7.2 replay/live inference continuation is superseded.

## Allowed maintenance

- documentation clarity
- compatibility fixes needed to keep the frozen module runnable as fallback/reference
- preservation of existing validation artifacts and test evidence

## Disallowed continuation

- new primary headed-demo feature work
- replay/live checkpoint continuation under the old lite-demo Phase 7.2 plan
- reintroducing lite-demo as the default headed backend without a new planning decision

## Replacement source of truth

Use the canonical docs in:

- `/home/jordan/code/runescape-rl/docs/active-reference/plan.md`
- `/home/jordan/code/runescape-rl/docs/source-of-truth/architecture.md`
- `/home/jordan/code/runescape-rl/docs/source-of-truth/contracts.md`
