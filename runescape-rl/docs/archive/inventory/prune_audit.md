# Prune Audit (Before Cleanup)

Date: 2026-03-16

## Before Size Snapshot

Top-level size report before cleanup (`du -x -h --max-depth=1 /home/jordan/code`):

- total: `51G`
- `RL`: `27G`
- `.workspace-tools`: `13G`
- `.cache`: `7.8G`
- `.gradle`: `2.3G`
- `fight-caves-RL`: `876M`
- `RSPS`: `366M`
- `.tmp`: `316M`
- `fight-caves-demo-lite`: `235M`
- `.artifacts`: `34M`
- `archive`: `24M`

Largest-file audit (selected) showed cache/toolchain binaries as primary offenders (`libtorch_*`, CUDA libs, LLVM binaries).

## Classification + Recommended Action (Before)

- `RSPS` (`source`, protected): keep; rename only for root consistency.
- `fight-caves-demo-lite` (`source + generated`): rename to `demo-env`; remove build/session outputs.
- `fight-caves-RL` (`source + generated`): move under `training-env/env`; remove build/temp/gradle products.
- `RL` (`mixed, mostly generated by size`): split source to `training-env/rl`; move/delete artifacts/venv/cache outputs.
- `.workspace-tools` (`generated tool-state`): remove from source root; relocate to runtime tool-state.
- `.tmp-build` (`generated`): remove.
- `archive` (`historical, non-runtime-source`): move out of source root.
- `.tmp` (`generated`): remove.
- `.grade` (`generated`): not present in current snapshot.
- `.config` (`local tool-state`): remove from source root.
- `.cache` (`generated cache`): remove from source root.
- `.artifacts` (`generated`): remove from source root.

## Applied Action Summary

- Root cleanup removed all hidden runtime dirs from source root:
  - `.cache`, `.config`, `.gradle`, `.tmp`, `.tmp-build`, `.workspace-tools`, `.artifacts`
- Product reorg:
  - `RSPS` -> `rsps`
  - `fight-caves-demo-lite` -> `demo-env`
  - `fight-caves-RL` + `RL` consolidated under `training-env/{env,rl}`
- Runtime outputs removed from source subtrees:
  - `demo-env/build`, `demo-env/session-logs`
  - `training-env/env` build/temp/gradle outputs
  - `training-env/rl` artifacts/venv/pytest cache
- Runtime root established at `/home/jordan/code/runescape-rl-runtime`.

## Top-Level Mapping (Current Directory Rules Applied)

- `RSPS`: renamed -> `rsps` (kept, internal content preserved).
- `fight-caves-demo-lite`: renamed -> `demo-env` (runtime outputs removed).
- `fight-caves-RL`: merged into `training-env/env`.
- `RL`: merged into `training-env/rl`.
- `.workspace-tools`: removed from source root; runtime toolchain now under `/home/jordan/code/runescape-rl-runtime/tool-state/workspace-tools`.
- `.tmp-build`: deleted from source root.
- `archive`: moved outside source root to `/home/jordan/code/runescape-rl-runtime/reports/archive-root`.
- `.tmp`: deleted from source root.
- `.grade`: not present in this workspace snapshot.
- `.config`: deleted from source root.
- `.cache`: deleted from source root.
- `.artifacts`: deleted from source root.

## Rationale

- Keep source-only structure navigable and reviewable.
- Externalize regenerable state to prevent source-root bloat recurrence.
- Preserve RSPS internals while changing only surrounding path ownership and runtime routing.
